macroScript tySplineCache
				category: "tyFlow" 
				internalCategory: "tyFlow" 
				tooltip: "tySplineCache" 
				ButtonText: "tySplineCache" 
				( 
					on execute do AddMod tySplineCache 
					on isEnabled return mcrUtils.ValidMod tySplineCache 
					) 
